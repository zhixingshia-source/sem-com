# Semantic Communication Project

融合 SeTok (语义分词) 与 CoDi (多模态生成) 的 6G 语义通信实验框架。

## 目录结构
- `configs/` : 模型与信道配置
- `checkpoints/` : 模型权重文件 (.pth)
- `src/` : 核心代码模块
- `core/` : CoDi 模型
- `scripts/` : 启动脚本
- `outputs/` : 运行输出 (自动生成)
